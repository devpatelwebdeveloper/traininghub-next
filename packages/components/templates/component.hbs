import React, { ReactNode } from 'react';
import { {{camelCase name}} } from './styles.css';

const {{pascalCase name}} = ()=>{
  return (
    <div data-testid="{{pascalCase name}}" className={}>
      {children}
    </div>
  );
};

export { {{pascalCase name}} as default}
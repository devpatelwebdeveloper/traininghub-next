import { Meta, Story, Canvas, ArgsTable } from '@storybook/addon-docs';

import {{pascalCase name}} from '.';

<Meta title="Core/{{ type }}/{{ name }}" component={ {{~pascalCase name~}} } />

<Canvas>
  <Story name="Intro">
    <{{pascalCase name}} />
  </Story>
</Canvas>